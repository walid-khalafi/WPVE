@{
    ViewData["Title"] = "Home Page";

    await Html.RenderPartialAsync("_LayoutHero", new HeroViewModel() { Name = "single-product" });

    await Html.RenderPartialAsync("_LayoutSingleProduct",new SingleProductViewModel() { Name="SingleProduct1"});

    await Html.RenderPartialAsync("_LayoutPartner");
    }

